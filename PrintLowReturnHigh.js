function numbers(arr) {
  var min = arr[0];
  var max = arr[0];
  for (var i = 1; i < arr.length; i++) {
  if (arr[i] < min) {
    min = arr[i];
  {
  if (arr[i] > max0 {
    max = arr[i];
  }
 }
console.log(min);
return(max);
}
